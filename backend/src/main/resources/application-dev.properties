# CORS configuration for development
quarkus.http.cors=true
quarkus.http.cors.origins=http://localhost:4200
quarkus.http.cors.headers=*
quarkus.http.cors.methods=*

# Development profile configuration

# Base application configuration
quarkus.http.port=8080

# Application info
quarkus.application.name=librarie
quarkus.application.version=1.0.0-SNAPSHOT

# Database configuration for development (PostgreSQL - NEVER use H2)
quarkus.datasource.db-kind=postgresql
quarkus.datasource.username=librarie_dev
quarkus.datasource.password=librarie_dev
quarkus.datasource.jdbc.url=jdbc:postgresql://localhost:5432/librarie_dev

# Flyway configuration - enabled for PostgreSQL
quarkus.flyway.migrate-at-start=true

# Hibernate configuration - validate for development with PostgreSQL
quarkus.hibernate-orm.database.generation=update
quarkus.hibernate-orm.log.sql=true

# OpenAPI/Swagger configuration - enabled in dev
quarkus.smallrye-openapi.enable=true
quarkus.swagger-ui.enable=true
quarkus.swagger-ui.always-include=true
quarkus.swagger-ui.path=/q/swagger-ui

# OIDC configuration placeholders for development
quarkus.oidc.auth-server-url=${OIDC_AUTH_SERVER_URL:http://localhost:8180/realms/dev-realm}
quarkus.oidc.client-id=${OIDC_CLIENT_ID:librarie-dev}
quarkus.oidc.credentials.secret=${OIDC_CLIENT_SECRET:dev-secret-placeholder}

# Dev-specific settings
quarkus.log.level=DEBUG
quarkus.log.console.enable=true